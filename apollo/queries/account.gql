query account ($domain: String!) {
  account(where: {
    domain: $domain
  } ) {
    name
    domain
    googleToken
    intercomToken
    stripeToken
    logo {
      url
    }
    users {
      email
    }
    dashboards {
      title
      blocks
      primaryColor
      backgroundColor
    }
  }
}
