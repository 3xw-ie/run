query account ($domain: String!) {
  account(where: {
    domain: $domain
  } ) {
    name
    domain
    intercomToken
    stripeToken
    logo {
      url
    }
    users {
      email
    }
    dashboards {
      title
      cards
      primaryColor
      backgroundColor
    }
  }
}
